ver limite de archivos abiertos
ulimit -n 

ver limite de usuario normal los soft
ulimit -S -n

ver limite root
ulimit -H -n

cambiar limte al root osea que uso el hard
ulimit -n hard
ulimit -n 2048
ulimit -n 
